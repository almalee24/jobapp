<% if current_user %>
    <header class="current_user_header">
            <div class="col-xs-8">
            <div>
                <h1><%= link_to "Magic Job Portal", root_path %></h1>
                <h6>Magical job opportunities right a your fingertips!</h6>
            </div>

            <ul class="main-nav">
                <li><%= link_to "All Jobs", positions_path%></li>
                <li>Post a job</li>
            </ul>
            </div>
    </header>
<% else  %>
    <header class="non_user_header">
        <div>
            <h1><%= link_to "Magic Job Portal", root_path %></h1>
        </div>
    </header>
<% end %>